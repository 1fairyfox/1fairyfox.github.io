# 2026-07-20

## Maintenance round-up — the 19th blogged; coins/standards adoption wave reconciled; docs library completed (0.20.3)

A scheduled maintenance pass. Started clean on `dev` at 0.20.2.

**References.** All four sibling mirrors fast-forwarded cleanly (no force-pushes). Every new commit
across all four sat on **2026-07-19** — one complete day (today is 07-20) — so one day post.

- **Pokered Save Editor 2** `4d2ffc8c → 5ae9dad3`, `0.43.4 → 0.43.6-alpha` (14 commits). The map screen
  learns to read a save's real progress: 0.43.5 retires the "Custom" state row (best-effort matching against
  dead-giveaway flags + missables + step byte; raw script steps now italic entries; `tst_map_states`), 0.43.6
  gives all 228 filter flags researched lifecycles and redraws them live on the canvas. Plus the global
  progression graph written down (70/2,560 flags reset, five classes, M0–M20 spine), adopting the shared-chrome
  bundle (2.0.0 → 2.2.1 coins), and a professional-tone documentation scrub.
- **Fairy Fox Games** `5d89b0c → a690442`, `0.24.2 → 0.24.4`. Skyline gets depth inside the mechanic (7th on
  the layer) + the hub standards batch adoption (coins initiative phase 1, docs-only).
- **Fairy Fox Stories** `82b5a5b → fb160a5`, `0.5.3 → 0.5.5`. The Blindfold Act ch.3 "The Tells" + standards
  adoption with the repo-hygiene link/tidy gates wired in; visual coins work deferred to a Phase 2 browser run.
- **Random AI Prompt** `2f1459f → 35b1370`, version unchanged at `2.60.1` — docs/adoption only: pulled the
  coins counter into its docs chrome + six new standards as reference notes. Tagged in the post; projects.yml
  left at 2.60.1 / Jul 12.

**Blogged.** New day post `_posts/2026-07-19-a-coin-that-travels-and-a-map-that-reads-the-evidence.md` — the
hub minted the coins layer + a batch of cross-brand standards on the 18th–19th, and by end of the 19th every
project had adopted them, alongside PSE2's map-reads-the-evidence work, Games' Skyline depth, and Stories'
Blindfold ch.3. Tags `[pokered-save-editor-2, fairyfox-games, fairyfox-stories, random-ai-prompt, site, update]`.

**Reconciled.** `_data/projects.yml` (PSE2 0.43.6, Games 0.24.4, Stories 0.5.5 → Jul 19; meta card 0.20.3 /
Jul 20 — it had drifted stale at 0.16.1 while the site was actually at 0.20.2). `_data/pulse.yml` (ticker +
bars advanced one day). **Docs library**: the ten standards added over 0.17.0–0.20.0 had no `_docs/` page —
added a readable summary page for each (orders 20–29). Checked `hub/registry.yml`, About, header/footer —
accurate, no change. No `downloads.yml` / `_projects/` (both retired). `hub/.last-seen.yml` — four markers
advanced, four round-up notes added; new sibling report files left for the inbound report-review pass.

**Ship.** Build-checked, committed on `dev`, released to `main`. VERSION `0.20.2 → 0.20.3`.
